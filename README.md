# C OpenGL integral visualizer

A code snippet which implements an `integral` helper function for integrating functions.

## Requirements

- OpenGL
- C standard libraries

## Usage

Run:
`gcc opengl_integral.c -o opengl_integral`
`./opengl_integral`